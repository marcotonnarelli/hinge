[project]
name = "vibe-mining"
version = "0.1.0"
description = "Executable design artifact for composing GitHub event-derived networks."
requires-python = ">=3.11"
dependencies = [
  "duckdb>=1.5.0",
  "pandas>=2.2.0",
  "pyarrow>=15.0.0",
  "jupyter>=1.1.0",
  "ipykernel>=6.29.0",
  "networkx>=3.2.0",
]

[tool.uv]
package = false
