"""Textual TUI — stub.

Plan: a single-screen app that lists registered sources, projections and
exporters; lets the user pick one of each; runs the pipeline; streams progress.
The frontend must compose stages via registry + runner only — no direct
imports from hinge.stages.
"""
