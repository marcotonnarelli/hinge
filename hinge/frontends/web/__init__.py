"""FastAPI web frontend — stub.

Plan: REST endpoints POST /ingest, POST /export, GET /projections, with an
SSE channel for progress events. Same composition rule as the CLI:
registry + runner only.
"""
